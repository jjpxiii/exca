services:
  - type: web
    name: excalidraw-collab
    plan: free
    env: node
    buildCommand: pnpm install && pnpm run build
    startCommand: pnpm start
    envVars:
      - key: DATABASE_URL
        sync: false
